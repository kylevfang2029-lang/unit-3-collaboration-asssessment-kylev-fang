1)they would have to make they own banch to work on and do there work and if there is anthing wrong they can check it with each other to see what is the problem with the code that went wrong.

2)Devs use baranches because it allows them to work on a repos and when they work on it it wont affect the main branch because they are working on a a diffrent branch.

3)git is a tool that we use for version control and it helps us by useinf it to control the codes. github is where the repository lives and the repository help us code.

4)the sequence should be git add . git commit -m "saveing work" git push
